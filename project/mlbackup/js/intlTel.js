var input = document.querySelector(".phone_input");
window.intlTelInput(input, {
    initialCountry: "mm",
    // geoIpLookup: function(callback) {
    //     $.get('https://ipinfo.io', function() {}, "jsonp").always(function(resp) {
    //         var countryCode = (resp && resp.country) ? resp.country : "us";
    //         callback(countryCode);
    //     });
    // },
});

var travel_phone_input = document.querySelector(".travel_phone_input");
window.intlTelInput(travel_phone_input, {
    initialCountry: "mm",
    // geoIpLookup: function(callback) {
    //     $.get('https://ipinfo.io', function() {}, "jsonp").always(function(resp) {
    //         var countryCode = (resp && resp.country) ? resp.country : "us";
    //         callback(countryCode);
    //     });
    // },
});

var food_phone_input = document.querySelector(".food_phone_input");
window.intlTelInput(food_phone_input, {
    initialCountry: "mm",
    // geoIpLookup: function(callback) {
    //     $.get('https://ipinfo.io', function() {}, "jsonp").always(function(resp) {
    //         var countryCode = (resp && resp.country) ? resp.country : "us";
    //         callback(countryCode);
    //     });
    // },
});

var hotel_phone_input = document.querySelector(".hotel_phone_input");
window.intlTelInput(hotel_phone_input, {
    initialCountry: "mm",
    // geoIpLookup: function(callback) {
    //     $.get('https://ipinfo.io', function() {}, "jsonp").always(function(resp) {
    //         var countryCode = (resp && resp.country) ? resp.country : "us";
    //         callback(countryCode);
    //     });
    // },
});

var leisure_phone_input = document.querySelector(".leisure_phone_input");
window.intlTelInput(leisure_phone_input, {
    initialCountry: "mm",
    // geoIpLookup: function(callback) {
    //     $.get('https://ipinfo.io', function() {}, "jsonp").always(function(resp) {
    //         var countryCode = (resp && resp.country) ? resp.country : "us";
    //         callback(countryCode);
    //     });
    // },
});